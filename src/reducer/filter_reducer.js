import {
    LOAD_PRODUCTS,
    UPDATE_SORT,
    SORT_PRODUCTS,
    UPDATE_FILTERS,
    FILTER_PRODUCTS,
    CLEAR_FILTERS,
} from '../action/action';

const filter_reducer = (state, action) => {
    switch (action.type) {
        default:
            return state;
    }
}